import{useEffect,useState}from"react";import{Menu,X}from"lucide-react";import{AnimatePresence,motion,useReducedMotion}from"framer-motion";import{navigation}from"./nav.data";import styles from"./MobileMenu.module.css";
export default function MobileMenu(){const[open,setOpen]=useState(false);const reduce=useReducedMotion();useEffect(()=>{if(!open)return;const previous=document.body.style.overflow;const closeOnEscape=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};document.body.style.overflow="hidden";window.addEventListener("keydown",closeOnEscape);return()=>{document.body.style.overflow=previous;window.removeEventListener("keydown",closeOnEscape)}},[open]);return <><button type="button" className={styles.menuButton} onClick={()=>setOpen(value=>!value)} aria-label={open?"Fechar menu":"Abrir menu"} aria-expanded={open} aria-controls="mobile-navigation">{open?<X size={24}/>:<Menu size={24}/>}</button><AnimatePresence>{open&&<><motion.div className={styles.overlay} initial={reduce?false:{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setOpen(false)}/><motion.nav id="mobile-navigation" aria-label="Menu mobile" className={styles.drawer} initial={reduce?false:{x:"100%"}} animate={{x:0}} exit={{x:"100%"}} transition={{duration:reduce?0:.3,ease:[.22,1,.36,1]}}>{navigation.map(item=><a key={item.id} href={`/#${item.href}`} className={styles.link} onClick={()=>setOpen(false)}>{item.label}</a>)}</motion.nav></>}</AnimatePresence></>}
