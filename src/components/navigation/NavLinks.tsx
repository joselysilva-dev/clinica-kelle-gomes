import{navigation}from"./nav.data";import styles from"./Navbar.module.css";export default function NavLinks(){return <nav aria-label="Menu principal"><ul className={styles.links}>{navigation.map(item=><li key={item.id}><a className={styles.linkButton} href={`/#${item.href}`}>{item.label}</a></li>)}</ul></nav>}
