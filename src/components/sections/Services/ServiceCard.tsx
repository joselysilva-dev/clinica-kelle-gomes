import{ArrowRight}from"lucide-react";import type{Service}from"./types";import styles from"./ServiceCard.module.css";
export default function ServiceCard({service}:{service:Service}){return <article className={styles.card}><div className={styles.imageWrapper}><img src={service.image} alt="" className={styles.image} loading="lazy" decoding="async"/><span className={styles.category}>{service.category}</span></div><div className={styles.content}><h3>{service.title}</h3><p>{service.description}</p><a className={styles.button} href="#contato">Saiba mais <ArrowRight size={16}/></a></div></article>}
